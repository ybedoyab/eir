# Synthetic suppliers

Fictional vendors with fictional catalogs. Phone numbers use the reserved
`+1555…` fictional range and are never dialled outside the synthetic voice
provider.

Catalogs are tuned so supplier choice is a real decision: for `MED-ENOX-40` the
cheapest vendor (Global Pharma Logistics) cannot ship the full requested
quantity, so the procurement agent must trade price against availability.
