# Google Cloud placeholders

Do not deploy from this folder yet. Intended future wiring:

- Cloud Run for the FastAPI API and (optionally) ADK API server
- Google Cloud Healthcare API / FHIR R4 stores
- Pub/Sub topics for domain events
- Gemini Enterprise: Agent Registry, Agent Runtime, Memory Bank, Agent Identity, Agent Gateway, Model Armor, Agent Observability
- Secret Manager for API keys (never commit credentials)

Local development uses in-memory adapters and `/mocks`.
